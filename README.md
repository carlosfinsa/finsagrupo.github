Creación de un formulario que devuelve el resultado númerico del test una vez realizado.

El código del funcionamiento esta en el .html en este caso index.html, el resto es lo necesario para la conexión de github
con netlify para que la web sea pública y esten conectados.
